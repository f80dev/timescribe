nodeLinker: node-modules

enableGlobalCache: false
enableTelemetry: false

# TimeScribe is a standalone project, NOT a workspace of any parent.
# Yarn 4 would otherwise detect /home/hhoareau/psy-site/yarn.lock as a parent workspace.
enableTransparentWorkspaces: false
